{
  "name": "dento-khata-desktop",
  "version": "0.1.6",
  "private": true,
  "description": "Dento Khata offline desktop app (Tauri) — bundles the backend + frontend into one installer.",
  "scripts": {
    "prepare-backend": "node scripts/prepare-backend.mjs",
    "release": "node scripts/release.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
